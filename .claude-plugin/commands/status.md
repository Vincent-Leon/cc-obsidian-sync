Show the current status of CC Obsidian Sync.

```bash
python3 ${CLAUDE_PLUGIN_ROOT}/scripts/cc-sync.py status
```

Present the output to the user in a clear format. If there are errors, suggest running `/cc-sync:setup` to fix configuration.
